function solve(stringInput, startIndex, count) {
    let result = "";
    result = stringInput.substring(startIndex, startIndex + count);
    console.log(result);
}   
solve("ASentance", 1, 8);
solve("ASentance", 1, 3);
